---
import { siteConfig } from "@/config/site";
import { cn } from "@/utils";

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<header
  class={cn(
    "flex items-center h-16 md:h-18 z-50 sticky top-0 bg-background",
    className
  )}
>
  <div class="container flex relative justify-between items-center w-full pr-8">
    <div class="flex gap-6 items-center md:gap-8 lg:gap-10">
      <a href="/" class="hidden items-center space-x-2 md:flex">
        <span class="hidden text-2xl uppercase sm:inline-block font-logo">
          {siteConfig.name}
        </span>
      </a>

      <slot name="mobile-nav-header" />

      <div class="hidden md:flex">
        <slot name="left-header" />
      </div>
    </div>

    <!-- logo mobile center -->
    <div
      class="absolute top-1/2 left-1/2 -translate-y-3 md:hidden -translate-x-18"
    >
      <a href="/" aria-label="Furd.dev homepage">
        <span class="text-2xl uppercase font-logo">{siteConfig.name}</span>
      </a>
    </div>

    <slot name="right-header" />
  </div>
</header>
